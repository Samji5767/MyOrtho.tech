/**
 * Scene scale for the 3D viewer.
 *
 * All mesh geometry is stored in millimetres (dental scan convention).
 * The viewer renders at 1 scene unit = 10 mm.
 */
export const MM_TO_SCENE = 0.1;
export const SCENE_TO_MM = 10;
